# Habilitar reescritura de URLs
# Options +FollowSymLinks is required by mod_rewrite on LiteSpeed / cPanel hosting.
# Without it the RewriteEngine is silently ignored for directory requests,
# causing the server to return HTTP 403 before PHP ever runs.
Options +FollowSymLinks

# Tell the server which file to use as a directory index.
# When a path that matches a real directory on disk is requested (e.g.
# /configuraciones) the server will try to serve its index.php instead
# of generating a 403 (Options -Indexes) or listing the directory.
DirectoryIndex index.php

RewriteEngine On

# Configuración de PHP (intentar sobrescribir open_basedir si es posible)
# Nota: Esto puede no funcionar en todos los servidores si está bloqueado por el admin
<IfModule mod_php.c>
    php_admin_value open_basedir none
</IfModule>

# Seguridad adicional - Headers HTTP
<IfModule mod_headers.c>
    # Prevenir Clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Protección XSS en navegadores antiguos
    Header always set X-XSS-Protection "1; mode=block"
    
    # Prevenir MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Política de Referrer (no enviar información sensible)
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Eliminar información del servidor
    Header unset X-Powered-By
    Header always unset X-Powered-By
</IfModule>

# Si el archivo no existe, redirigir al index.php
# Note: the !-d check is intentionally omitted so that even if a directory
# named after a route exists on the filesystem (e.g. 'configuraciones'),
# the request is still routed to PHP rather than being blocked by Apache
# with a 403 Forbidden (triggered by Options -Indexes).
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]

# Deshabilitar listado de directorios
Options -Indexes

# Proteger archivos de configuración
<FilesMatch "^\.">
    <IfModule mod_authz_core.c>
        # Apache 2.4+
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        # Apache 2.2
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# Proteger archivos sensibles
<FilesMatch "\.(sql|env|log|ini|md)$">
    <IfModule mod_authz_core.c>
        # Apache 2.4+
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        # Apache 2.2
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>
