# -----------------------------------------------------------------------
# OPTIONS
# Combine FollowSymLinks (required for mod_rewrite on LiteSpeed/cPanel)
# and -Indexes (disable directory listing) into a SINGLE Options directive.
# Having them on separate lines can confuse LiteSpeed: it may process the
# first "+FollowSymLinks" and then the later "-Indexes" in isolation,
# sometimes evaluating -Indexes BEFORE checking for a DirectoryIndex file
# and immediately returning 403 for real directories (e.g. /configuraciones).
# A single combined line avoids that race condition.
# -----------------------------------------------------------------------
Options +FollowSymLinks -Indexes

# Tell the server which file to use as a directory index.
# When a path that matches a real directory on disk is requested (e.g.
# /configuraciones) the server will serve its index.php stub instead of
# generating a 403 or listing the directory.
DirectoryIndex index.php

RewriteEngine On

# -----------------------------------------------------------------------
# LiteSpeed / cPanel SPECIFIC FIX: explicit early rule for directories
# that are also app routes.
#
# On LiteSpeed, requests for a real filesystem directory can be blocked
# (HTTP 403) before the general RewriteRule below is evaluated, because
# LiteSpeed resolves the -Indexes restriction at directory-check time, not
# at rewrite time.  Adding an unconditional rule that rewrites the bare
# directory URL to a concrete FILE (the stub index.php inside it) prevents
# LiteSpeed from ever reaching the -Indexes 403 path.
#
# If more route-directories are added in the future (e.g. /usuarios/),
# add a matching line here.
# -----------------------------------------------------------------------
RewriteRule ^configuraciones/?$ configuraciones/index.php [L]

# Configuración de PHP (intentar sobrescribir open_basedir si es posible)
# Nota: Esto puede no funcionar en todos los servidores si está bloqueado por el admin
<IfModule mod_php.c>
    php_admin_value open_basedir none
</IfModule>

# Seguridad adicional - Headers HTTP
<IfModule mod_headers.c>
    # Prevenir Clickjacking
    Header always set X-Frame-Options "SAMEORIGIN"
    
    # Protección XSS en navegadores antiguos
    Header always set X-XSS-Protection "1; mode=block"
    
    # Prevenir MIME type sniffing
    Header always set X-Content-Type-Options "nosniff"
    
    # Política de Referrer (no enviar información sensible)
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
    
    # Eliminar información del servidor
    Header unset X-Powered-By
    Header always unset X-Powered-By
</IfModule>

# Si el archivo no existe, redirigir al index.php
# Note: the !-d check is intentionally omitted so that even if a directory
# named after a route exists on the filesystem (e.g. 'configuraciones'),
# the request is still routed to PHP. The explicit RewriteRule added above
# handles the 'configuraciones' directory specifically; the general rule
# below catches all other non-file requests.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]

# (Directory listing is disabled by the combined Options directive at top)

# Proteger archivos de configuración
<FilesMatch "^\.">
    <IfModule mod_authz_core.c>
        # Apache 2.4+
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        # Apache 2.2
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>

# Proteger archivos sensibles
<FilesMatch "\.(sql|env|log|ini|md)$">
    <IfModule mod_authz_core.c>
        # Apache 2.4+
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        # Apache 2.2
        Order allow,deny
        Deny from all
    </IfModule>
</FilesMatch>
